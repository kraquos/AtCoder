#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>
#include <queue>
#define rep(i,n) for(int i=0;i<n;i++)
#define ll long long int
#define MAX 1000000007
using namespace std;
#define max(a, b) ((a) > (b) ? (a) : (b))




int main(void){
	int n,k;
	cin>>n>>k;
	int a[n];
	rep(i,n)cin>>a[i];

	for(int i=2;i<=k;i++){
		ll work0=0,work1=0;
	}
	return 0;
}
